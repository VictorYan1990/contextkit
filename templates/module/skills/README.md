# Skills

One folder per skill, each with a `SKILL.md` whose frontmatter `name` equals the
folder name and whose `description` says when to use it. See the central
`skill-authoring` skill for the contract.
