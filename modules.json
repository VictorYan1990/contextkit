{
  "$comment": "Registry of known contextkit sub-modules. `contextkit add <name>` resolves names here; any git URL also works. Each module is its own git repository following the module contract (docs/modules.md).",
  "modules": {}
}
