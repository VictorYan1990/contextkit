{
  "$comment": "Registry of known contextkit sub-modules. `contextkit add <name>` resolves names here; any git URL also works. Each module is its own git repository following the module contract (docs/modules.md).",
  "modules": {
    "personal": {
      "source": "https://github.com/VictorYan1990/contextkit-personal.git",
      "ref": "main",
      "description": "Victor's private profile and anything else too personal for the public kit (private repo — registered here for `add personal`, not for public use)"
    }
  }
}
